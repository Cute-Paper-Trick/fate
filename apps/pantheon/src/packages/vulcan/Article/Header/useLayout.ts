// import { useAspectRatio } from "./useAspectRatio";
// import { useCoverUrl } from "./useCoverUrl";

// export const useLayout = (coverKey?: string) => {
//   const coverUrl = useCoverUrl(coverKey);
//   const aspectRatio = useAspectRatio(coverUrl);

//   if (!aspectRatio || aspectRatio > 2) return "vertical";

//   return "horizontal";
// };
