'use client';

import { memo } from 'react';

import CollectionList from '@/packages/vulcan/Collection/List';

const DiscoverCollections = memo(() => {
  return <CollectionList />;
});

export default DiscoverCollections;
