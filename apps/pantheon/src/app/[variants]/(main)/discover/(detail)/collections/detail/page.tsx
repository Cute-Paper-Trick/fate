'use client';

import { memo } from 'react';

import CollectionDetail from '@/packages/vulcan/Collection/Detail';

const DiscoverCollectionDetail = memo(() => {
  return <CollectionDetail />;
});

export default DiscoverCollectionDetail;
