/* eslint-disable no-alert, no-console */
/**
* Generated by Kubb (https://kubb.dev/).
* Do not edit manually.
*/

import fetch from "@/lib/http/fetcher";
import type { AccountNeedChangePasswordQueryResponse } from "../../../types/accountTypes/AccountNeedChangePassword";
import type { RequestConfig, ResponseErrorConfig } from "@/lib/http/fetcher";

function getAccountNeedChangePasswordUrl() {
  const res = { method: 'GET', url: `/api/account/need_change_password` as const }  
  return res
}

/**
 * @summary 是否需要修改密码
 * {@link /api/account/need_change_password}
 */
export async function accountNeedChangePassword(config: Partial<RequestConfig> & { client?: typeof fetch } = {}) {
  const { client: request = fetch, ...requestConfig } = config  
  
  const res = await request<AccountNeedChangePasswordQueryResponse, ResponseErrorConfig<Error>, unknown>({ method : "GET", url : getAccountNeedChangePasswordUrl().url.toString(), ... requestConfig })  
  return res.data
}