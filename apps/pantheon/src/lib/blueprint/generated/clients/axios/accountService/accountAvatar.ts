/* eslint-disable no-alert, no-console */
/**
* Generated by Kubb (https://kubb.dev/).
* Do not edit manually.
*/

import fetch from "@/lib/http/fetcher";
import type { AccountAvatarMutationRequest, AccountAvatarMutationResponse } from "../../../types/accountTypes/AccountAvatar";
import type { RequestConfig, ResponseErrorConfig } from "@/lib/http/fetcher";

function getAccountAvatarUrl() {
  const res = { method: 'POST', url: `/api/account/avatar` as const }  
  return res
}

/**
 * @summary 上传头像
 * {@link /api/account/avatar}
 */
export async function accountAvatar(data: AccountAvatarMutationRequest, config: Partial<RequestConfig<AccountAvatarMutationRequest>> & { client?: typeof fetch } = {}) {
  const { client: request = fetch, ...requestConfig } = config  
  
  const requestData = data  
  const formData = new FormData()
  if (requestData) {
   Object.keys(requestData).forEach((key) => {
     const value = requestData[key as keyof typeof requestData];
     if (typeof value === 'string' || (value as unknown) instanceof Blob) {
       formData.append(key, value as unknown as string | Blob);
     } else {
       formData.append(key, JSON.stringify(value));
     }
   })
  }  
  const res = await request<AccountAvatarMutationResponse, ResponseErrorConfig<Error>, AccountAvatarMutationRequest>({ method : "POST", url : getAccountAvatarUrl().url.toString(), data : formData, ... requestConfig })  
  return res.data
}