/* eslint-disable no-alert, no-console */
/**
* Generated by Kubb (https://kubb.dev/).
* Do not edit manually.
*/

import fetch from "@/lib/http/fetcher";
import type { CommonUploadMutationRequest, CommonUploadMutationResponse } from "../../../types/commonTypes/CommonUpload";
import type { RequestConfig, ResponseErrorConfig } from "@/lib/http/fetcher";

function getCommonUploadUrl() {
  const res = { method: 'POST', url: `/api/common/upload` as const }  
  return res
}

/**
 * @summary 上传文件
 * {@link /api/common/upload}
 */
export async function commonUpload(data: CommonUploadMutationRequest, config: Partial<RequestConfig<CommonUploadMutationRequest>> & { client?: typeof fetch } = {}) {
  const { client: request = fetch, ...requestConfig } = config  
  
  const requestData = data  
  const formData = new FormData()
  if (requestData) {
   Object.keys(requestData).forEach((key) => {
     const value = requestData[key as keyof typeof requestData];
     if (typeof value === 'string' || (value as unknown) instanceof Blob) {
       formData.append(key, value as unknown as string | Blob);
     } else {
       formData.append(key, JSON.stringify(value));
     }
   })
  }  
  const res = await request<CommonUploadMutationResponse, ResponseErrorConfig<Error>, CommonUploadMutationRequest>({ method : "POST", url : getCommonUploadUrl().url.toString(), data : formData, ... requestConfig })  
  return res.data
}